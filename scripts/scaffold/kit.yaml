# 실습 키트 뼈대 — npm run new:kit으로 만들었다(PRACTICE_CASES 1절 필수 항목).
# 근거(sources)에는 content/sources.yaml에 등록된 ID만 적는다. 법령 조문·시행일을 여기에 직접 쓰지 않는다.
# 실제 기관 문서·실제 개인정보를 넣지 않는다. 전화번호는 010-0000-XXXX 형식만 쓴다.
id: {{kit_id}}
title: 작성 예정
track: core
sources: []            # sources.yaml ID(법령 조문 단위) 또는 fictional 자료 ID
guideline_frame:       # 지침서 5항목 틀
  role: 작성 예정
  scope: 작성 예정
  prohibited: 작성 예정
  output_format: 작성 예정
  when_unknown: 모르는 정보는 지어내지 말고 "확인 필요"라고 표시한다
context_files: []      # Project에 올릴 근거 자료(법령 발췌, 교육용 FAQ)
input_data: []         # 채팅에 첨부할 가상 입력 데이터(CSV)
eval_candidates: []    # 평가 문항 후보와 기대 답변(핵심 포인트 2개 + 근거)
form_template: []      # 업무용 폼 문항(선택 산출물용)
privacy_notes: 작성 예정
fictional_label: 교육용 가상 자료입니다. 실제 인물·기관과 관계없습니다.
