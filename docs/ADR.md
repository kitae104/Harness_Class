# 결정 기록 (Architecture & Course Decision Records)

## 철학
교육 원리는 오래 가고 제품 정보는 자주 바뀐다. 둘을 분리하고, 모든 수치와 출처는 한 곳에만 두며, 사람이 검토하지 않은 콘텐츠는 배포하지 않는다. 작동하는 가장 단순한 선택을 한다.

정보 우선순위: 사용자 결정 > 제안서 PDF > 공식 최신 문서 > 기타.

---

## A. 기술 결정 (ADR)

### ADR-001: 정적 사이트 — Astro + MDX + Content Collections [PROVISIONAL]
**결정**: Astro 정적 출력. 교시는 MDX, frontmatter는 zod 스키마.
**이유**: 페이지당 JS가 거의 없고, Markdown으로 강사가 직접 수정 가능하며, frontmatter 누락을 빌드에서 막는다. Vercel이 자동 인식한다.
**트레이드오프**: Next.js 대비 동적 기능이 약하다(필요 없음). 순수 HTML 대비 빌드 도구가 필요하다. 버전은 Phase 1에서 공식 문서로 확정.

### ADR-002: 서버·DB·로그인·분석 도구 없음
**결정**: 진행 체크는 브라우저 localStorage만 쓴다. 쿠키·분석 스크립트를 넣지 않는다.
**이유**: 교육자료 제공에 필요 없고, 공공기관 교육에서 개인정보 수집 논란을 원천 차단한다.
**트레이드오프**: 수강생 진도를 강사가 볼 수 없다(상호평가는 구글 폼으로 대체).

### ADR-003: 데이터 단일 원천
**결정**: 구조·수치는 `content/course.yaml`, 제품 정보·출처·외부 링크·용어·금지 표현은 각 등록부 yaml에만 둔다. docs는 설계 근거만 쓴다.
**이유**: 같은 정보가 여러 곳에 있으면 시간이 지나며 어긋난다. validator가 원천을 기준으로 검사할 수 있다.
**트레이드오프**: 문서만 읽어서는 수치를 알 수 없다(course.yaml을 함께 본다).

### ADR-004: 검사 담당 분리 — 등록부는 Python, MDX는 zod
**결정**: 등록부 yaml과 교차 규칙은 `scripts/validate_course.py`, MDX frontmatter는 zod가 검사한다. 같은 규칙을 두 곳에 구현하지 않는다.
**이유**: Phase 0부터 검사가 필요하고(Astro 이전), 규칙 이중화는 불일치를 만든다.
**트레이드오프**: Vercel 빌드는 Python 검사를 돌리지 않는다 → ADR-007의 CI로 보완.

### ADR-005: 사람 검토 관문과 해시
**결정**: 콘텐츠 status는 planned → draft → reviewed. AI는 draft까지만. 사람이 `review:approve`로 승인하면 콘텐츠 파일의 해시를 course.yaml의 `reviewed_hash`에 기록하고(ADR-011), 이후 파일이 바뀌면 오류로 처리한다. 해시는 줄바꿈을 LF로 정규화한 뒤 계산한다(Windows 작업 폴더의 CRLF와 CI의 LF가 달라도 같은 값). Production은 core 전부 reviewed일 때만.
**이유**: 법령 해석과 교육 적합성은 자동 검사로 보장할 수 없다. 승인 후 수정이 검토를 우회하지 못하게 한다.
**트레이드오프**: 사람 검토가 병목이 된다 → 콘텐츠 phase마다 human-review step을 두어 분산한다.

### ADR-006: Harness 실행기 개편 (execute.py)
**결정**: UTF-8 입출력, stdin 프롬프트 전달, core 문서(CLAUDE.md·PRD·ARCHITECTURE·ADR·CONTENT_GUIDE) 고정 + phase별 `guardrail_docs`, `depends_on`(main 병합 기준), phase 요약 전달, `allowed_paths` 검사, `HARNESS_EXECUTING=1`.
**이유**: Windows 한글 인코딩과 명령줄 길이 한계, docs 증가에 따른 프롬프트 비대화, phase 간 의존과 범위 이탈을 막기 위해.
**트레이드오프**: step 설계 시 allowed_paths와 guardrail_docs를 적어야 한다.

### ADR-007: 브랜치와 배포 — phase 브랜치 → PR → CI → main = Production
**결정**: phase는 한 번에 하나씩, `feat-<phase>` 브랜치에서 실행하고 PR로 main에 병합한다. GitHub Actions에서 `npm run verify`를 돌리고 main을 보호한다. 웹 편집도 새 브랜치 → PR로만 반영한다. Phase 0은 로컬 `feat-0-foundation` 브랜치. 저장소·Vercel 연결은 별도 phase `1b-repo-connect`로 두어, 원작자 허락(CD-15)을 기다리는 동안에도 콘텐츠 phase가 진행되게 한다(ADR-013).
**이유**: Vercel Preview로 확인한 것만 Production에 가게 하고, Python 검사를 CI에서 강제한다.
**트레이드오프**: 작은 수정도 PR을 거친다.

### ADR-008: 오프라인 번들과 인라인 스크립트
**결정**: `build:offline`으로 상대경로 정적 폴더를 만든다. 인터랙션 스크립트는 인라인 비모듈, 외부 CDN·웹폰트 금지.
**이유**: 교육망이 Vercel을 막을 수 있고(CD-05), Chrome은 file://에서 모듈 스크립트를 차단한다.
**트레이드오프**: 스크립트 번들 최적화를 포기한다(스크립트가 작아 영향 적음).

### ADR-009: Public 저장소의 공개 범위
**결정**: `references/`(원본 PDF)는 커밋하지 않는다. 강사 민감 메모는 저장소 밖에 둔다. 원작자 허락(CD-15) 전에는 push하지 않는다. 콘텐츠 라이선스는 허락 후 정한다 [TBD: OQ-13].
**이유**: 원본 PDF에 내부 운영 메모와 인적사항이 있고, 원본 프레임워크에 라이선스가 없다.
**트레이드오프**: 허락 전에는 CI·Preview를 쓸 수 없어 로컬 검증으로 대체한다.

### ADR-010: 호스팅 요금제 [PROVISIONAL]
**결정**: Vercel을 유지하되, Hobby 요금제는 비상업 용도만 허용하므로 유료 강의 운영에는 Pro 요금제 또는 대안 호스팅을 검토한다 [AS-OF: vercel-hobby-commercial] [TBD: OQ-09].
**이유**: 사용자 요구는 Vercel 배포이나, 약관상 강의 수익과 연결된 배포는 상업적 사용으로 볼 수 있다.
**트레이드오프**: Production 공개 전에 결정해야 한다(Phase 0~7 제작은 막지 않음).

### ADR-011: 검토 상태의 단일 원천 — course.yaml
**결정**: 교시·카드의 `status`와 `reviewed_hash`는 course.yaml에만 둔다. 콘텐츠 파일(MDX, 카드 파일)에는 상태를 쓰지 않는다. 콘텐츠 step은 course.yaml에서 자기 교시·카드의 status만 draft로 바꿀 수 있다(CLAUDE.md 예외).
**이유**: 상태가 두 곳에 있으면 어긋난다. 해시를 콘텐츠 파일 밖에 두면 해시를 기록하는 행위가 파일을 바꾸지 않는다. 진행 현황을 한 파일에서 볼 수 있다.
**트레이드오프**: 콘텐츠 step이 공유 등록부를 수정한다. "자기 항목만 수정"은 경로 검사로 강제할 수 없어 step 금지사항과 사람 검토로 보완한다.

### ADR-012: 브라우저 테스트 — Playwright(Chromium), verify와 분리
**결정**: Playwright로 복사 버튼, 이전/다음, 375px 화면, file:// 오프라인 번들, 외부 요청 차단 상태 표시를 검사한다. 명령은 `npm run test:e2e`로 `npm run verify`와 분리한다.
**이유**: 오프라인 제약(ADR-008)은 브라우저로만 검증할 수 있다. 느린 테스트를 매 step AC에 넣지 않는다.
**트레이드오프**: 브라우저 설치(약 150MB)가 필요하다. 오프라인·UI를 바꾸는 step과 배포 관문에서만 실행한다.

### ADR-013: 저장소 연결을 별도 phase로
**결정**: GitHub 저장소 연결, origin 교체, Vercel 연결, 브랜치 보호는 `1b-repo-connect` phase에서 한다. 배포 phase만 이 phase에 의존한다.
**이유**: execute.py는 선행 phase의 모든 step이 completed여야 다음 phase를 실행한다. 원작자 허락을 기다리는 step을 Phase 1에 두면 이후 콘텐츠 제작 전체가 멈춘다.
**트레이드오프**: 허락 전까지 CI와 Vercel Preview가 없어 로컬 `npm run verify`·`npm run preview`로 대신한다.

---

## B. 교육 설계 결정 (CD) — 사용자 결정, 2026-09-24

| ID | 결정 | PDF 원문 | 근거 |
|---|---|---|---|
| CD-01 | 개인 ChatGPT Plus 계정 기준. 데이터 제어·임시 채팅은 수강생이 직접 설정. 교육에는 가상·공개 자료만, 실무 적용은 기관 보안 협의 후 | ChatGPT 무료 계정, 파일 5개, "모두 무료" | 사용자 요구 1·2 |
| CD-02 | Public 저장소. 강사 민감 메모는 저장소 밖, 공개 가능한 진행 안내만 `content/instructor/` | 해당 없음(PDF 11절은 내부 메모) | 사용자 결정 |
| CD-03 | 7+7교시(교시 50분), 교시당 핵심 활동 35~40분 + 버퍼. 넘치는 내용은 "더 해보기" | 교육내용 표 7+8=15시간, 시간표 7+7 | 사용자 결정(초보자 여유) |
| CD-04 | 실제 법령을 조문 단위로 인용하고 시행일 표기. 구청 편람은 "법령 기반 교육용 자료"로 대체. → Day 1 실습 규정 자료는 CD-21(교육용 가상 규정)로 변경, 교시 본문의 법령 인용은 유지 | "주민등록법·시행령과 구 민원편람" | 사용자 결정 |
| CD-05 | Vercel 불가 시 오프라인 번들 또는 강사 PC 로컬 실행 | 해당 없음 | 사용자 결정 |
| CD-06 | 채점 문항당 1점: 정확과 출처를 모두 충족해야 1점, 금지 위반 0점, 20점 만점. "자료에 없음" 문항은 "확인 필요"면 1점(출처 요건 면제). 8열 채점표 유지 | 정확 1점 + 출처 1점인데 20문항 20점 만점(모순) | 사용자 결정 |
| CD-07 | Day 2 입력 데이터는 CSV/xlsx를 채팅에 첨부. Project 파일은 근거 전용. 붙여넣기는 플랜 B | 시트 표를 복사해 붙여넣기 | 사용자 결정 |
| CD-08 | 제안서 개정본을 산출물로 만든다(`deliverables/course_proposal_v2.md`, CD ID 참조 대조표) | 해당 없음 | 사용자 결정 |
| CD-09 | 업무용 구글 폼은 선택 산출물. 설계서 ③도구 칸은 필수 | 업무용 구글 폼과 대장 시트를 가지고 돌아감 | 사용자 결정 |
| CD-10 | 내 업무 실습은 예시 5종 중 선택(규정 Q&A, 민원 답변 초안(Day 1과 다른 유형), 현장 점검, 수요조사, 회의록). 키트 제공, 실제 기관 문서 없음 | 본인 업무 1개 선정 | 사용자 결정 |
| CD-11 | 입과 안내와 수료 설문은 교시 안(D1-1, D2-7) | 입과 안내는 1교시, 수료 설문은 7교시 | 사용자 결정 |
| CD-12 | 규정 Q&A 키트는 일반형 1종, 기준 법령 1개 [PROVISIONAL: 국가공무원 복무규정]. 지방직·기관 세부는 "확인 필요" 사례. → 실습 근거는 CD-23(가상 복무 규정)으로 변경 | 규정 Q&A 비서(인사·복무) | 사용자 결정 |
| CD-13 | 현장 점검 키트 근거는 실제 법령 [PROVISIONAL: 재난 및 안전관리 기본법 안전점검 조문]. → 실습 근거는 CD-23(가상 시설 안전점검 규정)으로 변경 | 현장 점검 보고 폼 | 사용자 결정 |
| CD-14 | Codex는 정규 교시에서 제외, `/optional/codex` 1페이지 | 해당 없음 | 사용자 요구 3·4 |
| CD-15 | 원본 프레임워크에 라이선스가 없으므로 원작자 허락 후 공개. 허락 전 Public push 금지 | 해당 없음 | 사용자 결정 |

### 제안서 대비 추가 변경 (Gap Analysis에서 도출, 사용자 검토 완료)
| ID | 변경 | PDF 원문 |
|---|---|---|
| CD-16 | Day 1 평가세트 20문항은 미리 제공하고 수강생은 3문항 추가. 기대 답변은 "핵심 포인트 2개 + 근거 조문" | 수강생이 20문항과 기대 답변 작성 |
| CD-17 | 평가는 **답변과 채점을 분리**한다. ① 답변: Project의 새 대화에 **질문만** 넣어 두 번(10·13문항)에 나눠 받는다. ② 채점: **Project 밖** 비개인화 임시 채팅에서 질문·기대 답변·받은 답을 넣어 채점한다. 사람이 3문항 표본을 확인한다. 이유: 답변 단계에 기대 답변을 넣으면 점수가 부풀고, Project 안에서 채점하면 프로젝트 메모리로 이후 재평가가 정답을 참조할 수 있다(2026-09-24 설계 보강 리뷰) | 20문항과 기대 답변을 한 번에 주고 답변·자기채점 |
| CD-18 | 의도된 결함은 수강생이 작성하지 않는 자료(교육용 FAQ 누락, 범위 애매 문항)에만 두고, "일부러 단순하게 만든 초안"임을 공개 | 해당 없음 |
| CD-19 | Day 2 분석 데이터는 강사 공용 폼(역할카드) + 제공 CSV | 수강생끼리 서로의 폼에 응답 |
| CD-20 | 맞춤형 GPT는 가르치지 않는다. 동료 공유는 프로젝트 공유로 안내 [AS-OF: chatgpt-custom-gpt-creation] | 해당 없음 |
| CD-21 | Day 1 실습의 규정 자료는 실제 법령 원문을 옮기지 않고, 실제 법령의 구조와 취지를 참고해 만든 **교육용 가상 규정**(조문 번호 형식)과 교육용 FAQ로 한다. 가상 규정은 교육 목적의 합성 자료임을 밝히고, 실제 법령 원문이나 법적 효력이 있는 규정처럼 표현하지 않는다. AI가 키트 step에서 만들고 강사 본인이 검토·승인한다(H-01). 실제 법령은 교시 본문에서 "실제 업무에서 확인할 법령"으로만 `<Source>` 인용해 가상 실습 자료와 구분한다. 이유: 원문 확보(OQ-22)와 해석 검토 부담이 Day 1 제작을 막고, 실습 정답을 강사가 통제할 수 있어야 한다(2026-09-24 사용자 결정) | 주민등록법·시행령과 구 민원편람을 근거 자료로 사용 |
| CD-22 | 2-day1-content는 **선승인 후재검토**와 **OQ-03 보수 절차**로 끝까지 진행한다. ① step 10·12는 AI 사전 검토 결과를 근거로 사용자가 먼저 `review:approve`를 실행한다(승인은 계속 사람만 한다, ADR-011). 교육 내용의 강사 재검토와 카드 실사용 테스트는 [TBD: OQ-23]로 추적하고, 끝나기 전에는 개설·Production 공개를 하지 않는다. `tested_at`은 실제로 테스트하기 전까지 비워 둔다. ② OQ-03(같은 Project의 다른 대화가 평가에 주는 영향)은 확인 전이므로 **영향이 있다고 가정**한다. D1-06은 등록부 대안(평가 전 연습 대화 정리, 정답 내용을 Project에 넣지 않기)으로 작성하고, 실사용 결과가 나오면 D1-06을 다시 검토한다. 이유: 사용자가 콘텐츠를 먼저 끝까지 작성한 뒤 결과를 보고 고치기로 했다(2026-09-25 사용자 결정) | 해당 없음 |
| CD-23 | CD-21(교육용 가상 규정)을 **Day 2 키트 전체**로 넓힌다. `rule-qa`는 가상 복무 규정(근무시간·연가·병가), `field-inspection`은 가상 시설 안전점검 규정, `civil-draft`는 **대형 생활폐기물 배출 신고**(Day 1 전입신고와 다른 유형)의 가상 규정을 쓴다. `demand-survey`·`meeting-minutes`는 규정 없이 가상 양식만 쓴다. 실제 법령(`law-civil-service-conduct`, `law-disaster-safety`)은 교시 본문의 "실제 업무에서 확인할 법령"으로만 인용한다. 이유: 사용자의 원래 요구(실습은 가상 자료로)와 Day 1 결정의 일관성, 원문 확보·해석 검토 부담 제거(2026-09-25 사용자 위임 결정, OQ-20·OQ-21 해결) | 국가공무원 복무규정, 재난 및 안전관리 기본법 조문을 근거 자료로 사용 |
| CD-24 | 남은 강의 자료(Day 2 교시·카드·키트, 안내 페이지)는 **AI가 세션에서 직접 작성**해 draft까지 완성하고, 사람 검토는 한꺼번에 나중에 한다. ① Day 1이 CD-22로 main에 병합·공개되기 전이라 execute.py(선행 phase를 main에서 확인)를 쓸 수 없다. ② 프로젝트형 기준 예시(D2-5) 승인 전에도 D2-6·D2-7을 함께 draft로 만든다(CONTENT_GUIDE 2절의 예외). ③ 모든 새 항목은 OQ-23 재검토 대상에 들어가고, 승인 전에는 개설·Production 공개를 하지 않는다. 이유: 사용자가 "전체 강의 자료를 먼저 완성하고 이후에 확인하며 고친다"고 결정했다(2026-09-25) | 해당 없음 |
| CD-25 | 강의 준비 중 사람이 확인·설정해야 할 곳은 **강의 준비 표시**로 남기고, 준비가 끝나면 한 번에 지운다. 본문(MDX)은 `<PrepNote>` 블록, 강사 안내(.md)는 `## 강의 준비` 절, 설명이 긴 작업은 저장소 루트 `prep/` 폴더의 안내 파일에 둔다. `npm run prep:list`로 남은 표시를 모으고 `npm run prep:strip`으로 지운다. reviewed 교시 본문에는 넣지 않는다(해시가 깨짐) — 그 교시의 준비 항목은 강사 안내나 `prep/`에 둔다. 개설 전 관문에 "`prep:list` 0건"을 더한다. 이유: 사용자가 준비 중 확인할 곳을 강의 자료 안에서 보고, 준비 후 쉽게 지우기를 원했다(2026-09-25) | 해당 없음 |
