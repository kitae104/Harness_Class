---
# AI에게 도움받기 카드 — 첨부한 접수 표 분류·요약(PROMPT_GUIDE 2~4절, 8절 제안서 프롬프트 ① 이관).
# 바꾼 점: 표를 입력창에 붙여 넣던 것을 채팅 첨부로 바꿨다(CD-07). 붙여 넣기는 플랜 B로 next에 안내한다.
# 분류 기준은 카드에 붙이지 않는다. Project 파일(분류 기준표)에 있는 근거를 이름으로 가리킨다.
# 카드가 짧아지는 다음 단계(규칙을 지침으로 옮기기)는 next에 안내한다(PRACTICE_DESIGN 6절 3번).
# 기술 단계: 분류는 D2-2가 바꿔 쓰기(PRACTICE_DESIGN 5절). 대괄호 세 곳을 바꾼다.
# 상태(status)는 이 파일이 아니라 course.yaml에만 있다.
id: card-classify-attachment
stuck_point: sp-d2-02-c
where: project-chat
when: 민원 답변 비서 Project에 분류 기준표를 올린 뒤, 여러 건의 접수 표 파일(CSV)을 한 번에 분류·요약할 때 씁니다. 표 파일을 채팅에 첨부하고 이 요청을 함께 보냅니다.
input: 첨부 전에 개인정보를 지운 표 파일(CSV)을 채팅에 첨부합니다. 대괄호 세 곳에 업무 이름, Project 파일로 올린 기준 자료 이름, 결과 표의 열을 넣습니다. 실제 개인정보를 넣지 마세요. 실습에서는 교육용 가상 자료만 씁니다.
l3_structure: |
  [역할] 너는 ○○ 업무의 접수 건을 분류하는 ○○다.
  [입력] 첨부한 표 파일은 이번에 처리할 입력 데이터다. 분류 기준은 Project에 올린 ○○ 자료다.
  [규칙] 기준 자료에 적힌 기준으로만 분류한다 / 개인정보를 요약과 결과 표에 옮기지 않는다
  [출력 형식] 정한 열 순서의 표 하나와, 표 아래 확인 필요 건수
  [모를 때] 모르는 정보는 지어내지 말고 "확인 필요"라고 표시하라.
l2_template: |
  너는 [업무 이름]의 접수 건을 분류하는 비서다.
  입력: 이 메시지에 첨부한 표 파일은 이번에 처리할 입력 데이터다. 분류 기준은 이 Project에 올린 [기준 자료 이름]이다.
  규칙:
  - 유형·긴급도·담당 부서·확인 필요는 기준 자료에 적힌 기준으로만 정한다. 유형은 신청인이 고른 값이 아니라 내용을 보고 정한다.
  - 기준 자료로 판단할 수 없는 건은 짐작하지 말고 확인 필요를 "예"로 두고, 기준 자료의 이유 기호를 적는다.
  - 이름, 상세 주소, 연락처 같은 개인정보는 요약과 결과 표에 옮기지 않는다. 개인정보가 있던 건은 비고에 "개인정보 포함(옮기지 않음)"이라고만 적는다.
  - 첨부한 표의 모든 행을 빠짐없이 한 줄씩 분류한다. 답변 초안은 쓰지 않는다.
  - 입력에 실제 개인정보가 보이면 먼저 "실제 개인정보를 넣지 마세요"라고 알린다.
  출력 형식: 아래 열 순서의 표 하나로 답한다. 표 앞에 설명을 붙이지 않는다.
  [결과 표의 열]
  표 아래에 "확인 필요 N건"과 그 번호를 적는다.
  모를 때: 모르는 정보는 지어내지 말고 "확인 필요"라고 표시하라.
l1_full: |
  너는 누리1동 행정복지센터(교육용 가상) 민원 접수의 접수 건을 분류하는 비서다.
  입력: 이 메시지에 첨부한 표 파일은 이번에 처리할 입력 데이터다. 분류 기준은 이 Project에 올린 누리1동 민원 분류 기준표다.
  규칙:
  - 유형·긴급도·담당 부서·확인 필요는 기준 자료에 적힌 기준으로만 정한다. 유형은 신청인이 고른 값이 아니라 내용을 보고 정한다.
  - 기준 자료로 판단할 수 없는 건은 짐작하지 말고 확인 필요를 "예"로 두고, 기준 자료의 이유 기호를 적는다.
  - 이름, 상세 주소, 연락처 같은 개인정보는 요약과 결과 표에 옮기지 않는다. 개인정보가 있던 건은 비고에 "개인정보 포함(옮기지 않음)"이라고만 적는다.
  - 첨부한 표의 모든 행을 빠짐없이 한 줄씩 분류한다. 답변 초안은 쓰지 않는다.
  - 입력에 실제 개인정보가 보이면 먼저 "실제 개인정보를 넣지 마세요"라고 알린다.
  출력 형식: 아래 열 순서의 표 하나로 답한다. 표 앞에 설명을 붙이지 않는다.
  번호 | 접수일 | 유형(내용 기준) | 긴급도 | 담당 부서 | 한 줄 요약 | 확인 필요(예/아니오) | 확인 필요 이유(A~E) | 비고
  표 아래에 "확인 필요 N건"과 그 번호를 적는다.
  모를 때: 모르는 정보는 지어내지 말고 "확인 필요"라고 표시하라.
default_level: 2
line_notes:
  - {line: "너는 ○○ 업무의 접수 건을 분류하는 비서다", element: instructions, why: 이번 대화에서 할 일을 분류로 좁힙니다. 민원에 답하는 평소 역할과 섞이지 않게 합니다.}
  - {line: "첨부한 표 파일은 이번에 처리할 입력 데이터다", element: tools, why: 입력 데이터는 채팅 첨부로 넣고 그 대화에서만 씁니다. Project 파일에 두면 근거와 섞입니다.}
  - {line: "분류 기준은 이 Project에 올린 기준 자료다", element: context, why: 분류 기준을 카드에 붙이지 않고 근거 자료를 이름으로 가리킵니다. 기준이 바뀌면 파일만 바꿉니다.}
  - {line: "유형은 신청인이 고른 값이 아니라 내용을 보고 정한다", element: instructions, why: 민원인이 유형을 잘못 고르는 일이 흔합니다. 고른 값만 믿으면 담당 부서가 틀립니다.}
  - {line: "판단할 수 없는 건은 확인 필요를 예로 두고 이유 기호를 적는다", element: guardrails, why: AI가 결론을 내지 않고 사람에게 넘길 건을 표시합니다. 이유 기호가 있어야 사람이 빨리 검토합니다.}
  - {line: "개인정보는 요약과 결과 표에 옮기지 않는다", element: guardrails, why: 첨부 전에 놓친 개인정보가 있어도 대장으로 옮겨 가지 않게 막는 두 번째 장치입니다.}
  - {line: "모든 행을 빠짐없이 한 줄씩 분류한다", element: evals, why: 여러 건 처리에서 가장 흔한 오류는 빠진 행입니다. 행 수를 세어 확인할 수 있게 합니다.}
  - {line: "정한 열 순서의 표 하나로 답한다", element: observability, why: 열 순서가 같아야 결과를 그대로 시트에 붙여 분류 대장으로 쌓을 수 있습니다.}
  - {line: "표 아래에 확인 필요 N건과 그 번호를 적는다", element: observability, why: 사람이 검토할 분량이 한눈에 보입니다. 표의 예 개수와 맞는지 세어 봅니다.}
  - {line: "모르는 정보는 지어내지 말고 확인 필요라고 표시하라", element: guardrails, why: 기준표에 없는 부서나 기한을 짐작으로 메우지 않게 합니다.}
replace: ["[업무 이름]", "[기준 자료 이름]", "[결과 표의 열]"]
check:
  - 결과 표의 행 수가 첨부한 표의 건수(머리글 제외)와 같다
  - 유형과 담당 부서가 기준 자료에 있는 값뿐이다
  - 확인 필요가 "예"인 건마다 이유 기호가 있고, 표 아래 건수가 표의 "예" 개수와 같다
  - 이름·상세 주소·연락처가 한 줄 요약과 표 어디에도 없다
do_not_trust:
  - 표 아래의 "확인 필요 N건"을 그대로 믿지 않습니다. 표에서 "예"를 직접 세어 봅니다.
  - 확인 필요 이유 기호가 맞는지 믿지 않습니다. 기준표의 기준 문장과 한 건씩 대조합니다.
  - 짧고 모호한 민원을 "단순 문의"로 가볍게 요약할 수 있습니다. 원문과 요약을 나란히 봅니다.
  - 긴급도는 경계에 있는 건에서 한 칸씩 다를 수 있습니다. 기준표의 어느 줄로 판단했는지 묻습니다.
next: 결과 표를 하네스 대장의 분류 대장에 붙이고, 확인 필요 건을 기준표로 검토합니다. 표가 깨지면 "표가 깨질 때" 카드를 씁니다. 이 카드의 규칙과 출력 형식은 매번 같으므로 Project 지침으로 옮기면 다음부터 요청이 한 줄로 짧아집니다. 첨부가 막히면(플랜 B) 표 파일을 시트나 메모장으로 열어 개인정보를 지운 표를 복사해 입력창에 붙여 넣고, "이 메시지에 첨부한 표 파일"을 "아래에 붙여 넣은 표"로 바꿔 보냅니다.
tested_at: null
tested_by: null
---
