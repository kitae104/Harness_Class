---
# AI에게 도움받기 카드 — 공통 문제 해결: 출력이 끊기거나 너무 길 때(TOOL_GUIDE 5·6절, PROMPT_GUIDE 2~4절).
# 공통 카드(lesson: common)라 특정 교시의 내용을 담지 않는다. 처음 만나는 문제라 기본 단계는 따라 쓰기.
# 붙여 넣는 곳은 답이 끊긴 그 Project 대화다. 새 대화를 열면 앞의 답을 이어 쓸 수 없다.
# 이어 쓰기만 요청한다. 답을 줄이거나 형식을 바꾸게 하지 않는다 — 평가 중이면 조건이 달라져 점수를 비교할 수 없다.
# 입력이 너무 길면 나눠 넣는 것이 해결책이다. 카드를 길게 만들지 않고 next에 안내한다.
# 상태(status)는 이 파일이 아니라 course.yaml에만 있다.
id: card-help-long-output
stuck_point: sp-d1-06-a
where: project-chat
when: 여러 문항을 한 번에 넣었더니 답이 중간에 끊겼거나, 답이 너무 길어 끝까지 받지 못했을 때 씁니다. 처음부터 다시 묻지 않고, 끊긴 곳부터 이어서 받습니다.
input: 대괄호 자리에 답이 끊긴 곳(마지막으로 받은 문항 번호나 마지막 문장)을 적습니다. 이 카드는 답이 끊긴 그 대화에 이어서 붙여 넣습니다. 실제 개인정보를 넣지 마세요.
l3_structure: |
  [역할] 너는 방금 답하던 ○○다.
  [입력] 방금 답이 [끊긴 곳]에서 끊겼다.
  [규칙] 앞에서 쓴 내용은 다시 쓰지 않는다 / 같은 형식·같은 기준으로 이어 쓴다 / 줄이거나 요약하지 않는다
  [출력 형식] 끊긴 곳부터 이어서, 다 못 쓰면 마지막 줄에 다음 시작 위치
  [모를 때] 모르는 정보는 지어내지 말고 "확인 필요"라고 표시하라.
l2_template: |
  너는 이 대화에서 방금 답하던 비서다. 방금 답이 [끊긴 곳]에서 끊겼다.
  규칙:
  - 앞에서 이미 쓴 내용은 다시 쓰지 말고, 끊긴 곳부터 이어서 써라.
  - 앞의 답과 같은 형식, 같은 기준으로 써라. 내용을 줄이거나 요약하지 마라.
  - 이번에도 다 쓰기 어려우면 문항이나 문단이 끝나는 곳에서 멈추고, 마지막 줄에 "다음은 ○○부터"라고 적어라.
  - 입력에 실제 개인정보가 보이면 먼저 "실제 개인정보를 넣지 마세요"라고 알려라.
  출력 형식: 끊긴 곳의 번호나 문장부터 이어서 쓴다.
  모를 때: 모르는 정보는 지어내지 말고 "확인 필요"라고 표시하라.
l1_full: |
  너는 이 대화에서 방금 답하던 비서다. 방금 답이 [끊긴 곳]에서 끊겼다.
  규칙:
  - 앞에서 이미 쓴 내용은 다시 쓰지 말고, 끊긴 곳부터 이어서 써라.
  - 앞의 답과 같은 형식, 같은 기준으로 써라. 내용을 줄이거나 요약하지 마라.
  - 이번에도 다 쓰기 어려우면 문항이나 문단이 끝나는 곳에서 멈추고, 마지막 줄에 "다음은 ○○부터"라고 적어라.
  - 입력에 실제 개인정보가 보이면 먼저 "실제 개인정보를 넣지 마세요"라고 알려라.
  출력 형식: 끊긴 곳의 번호나 문장부터 이어서 쓴다.
  모를 때: 모르는 정보는 지어내지 말고 "확인 필요"라고 표시하라.
default_level: 1
line_notes:
  - {line: "너는 이 대화에서 방금 답하던 비서다", element: instructions, why: 새 역할을 주지 않고 같은 대화의 같은 비서로 이어 쓰게 합니다. 지침과 자료가 그대로 적용됩니다.}
  - {line: "앞에서 이미 쓴 내용은 다시 쓰지 말고, 끊긴 곳부터 이어서 써라", element: instructions, why: 처음부터 다시 쓰면 또 같은 곳에서 끊깁니다. 끊긴 곳을 알려 주면 남은 부분만 받습니다.}
  - {line: "같은 형식, 같은 기준으로 써라. 내용을 줄이거나 요약하지 마라", element: evals, why: 뒤쪽 답만 짧아지면 앞뒤 답을 같은 기준으로 비교할 수 없습니다. 평가 중이라면 점수가 달라집니다.}
  - {line: "마지막 줄에 다음은 ○○부터라고 적어라", element: observability, why: 어디까지 받았는지 기록이 남습니다. 다시 끊겨도 이어 받을 곳을 바로 알 수 있습니다.}
  - {line: "모르는 정보는 지어내지 말고 확인 필요라고 표시하라", element: guardrails, why: 이어 쓰다가 빈 곳을 짐작으로 메우지 않게 하는 규칙입니다.}
replace: ["[끊긴 곳]"]
check:
  - 이어 쓴 답이 끊긴 곳의 바로 다음부터 시작하고, 앞의 내용을 되풀이하지 않는다
  - 앞의 답과 형식(번호, 근거 줄 등)이 같다
  - 받은 답을 모두 모으면 빠진 번호나 문단이 없다
do_not_trust:
  - AI가 "모두 답했습니다"라고 해도 믿지 않습니다. 번호를 처음부터 끝까지 세어 빠진 것이 없는지 봅니다.
  - 이어 쓴 부분이 앞보다 짧거나 근거 줄이 빠졌다면 요약된 것입니다. 그 부분은 나눠서 다시 받습니다.
next: 받은 답을 모두 모아 원래 하려던 일(기록, 채점 등)을 이어 합니다. 두 번 이상 끊기면 이어 받기를 멈추고, 입력을 다섯 개 정도씩 나눠 새 대화에서 다시 넣습니다. 채점 대화에서 끊겼을 때도 문항을 나눠 채점합니다.
tested_at: null
tested_by: null
---
